import { BsbMaskDirective } from './bsbMask.directive';

describe('HighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BsbMaskDirective();
    expect(directive).toBeTruthy();
  });
});
